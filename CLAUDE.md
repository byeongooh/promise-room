# Applan

친구와 플랜을 잡고, 지킨 만큼 사과가 익는 앱. 원래 이름은 Promise Room이었고
"약속 = 입장권(티켓)"이 축이었는데, 지금은 "사람 = 사과"로 축을 옮긴 Applan이다.
지각 벌칙(자유 텍스트)은 없앴고, 그 자리를 수확에서 나오는 독사과가 대신할
예정이다(수확 기능은 아직 없다).

**최종 목표는 React Native 앱**이다. 지금의 웹앱은 징검다리이고, 참여자끼리
**실시간 위치를 공유**하는 것이 종착점이다. 그래서 웹 전용으로만 쓰이고 버려질
작업(예: iOS 웹 푸시를 위한 PWA 우회)에는 시간을 쓰지 않는다.

- 프로덕션: https://applan.vercel.app
  (옛 주소 https://promise-room.vercel.app 도 그대로 열린다 — 이미 보낸
   초대 링크를 살려두려고 안 지웠다)
- 저장소: https://github.com/byeongooh/promise-room
- Firebase 프로젝트: `promise-room`

  **프로덕션 도메인은 2026-08-21에 applan으로 옮겼다**(아래 "도메인 변경" 절).
  저장소와 Firebase는 아직 옛 이름이고, 둘은 사정이 다르다 — GitHub 저장소명은
  바꿀 수 있지만 남의 링크·클론이 깨질 수 있어 미정이고, Firebase 프로젝트 ID는
  애초에 이름을 바꿀 방법이 없다(새 프로젝트를 만들어 데이터를 통째로 옮겨야
  한다). 자세한 내용은 아래 "이름을 어디까지 바꿀까" 절 참고.

## 기술 스택

Next.js 16 (App Router) · TypeScript · Tailwind v4 + shadcn/ui ·
NextAuth v4(카카오 로그인만) · Firebase Firestore + Admin SDK · Vercel

## 새 컴퓨터에서 시작하기

```bash
git clone https://github.com/byeongooh/promise-room.git
cd promise-room
npm install
```

그다음 **`.env.local`을 손으로 만들어야 한다.** 이 파일은 깃에 올라가지 않는다
(비밀 값이라 올리면 안 된다). 필요한 이름은 아래와 같고, 값은 Vercel →
Settings → Environment Variables에 등록되어 있다. 단 **Sensitive로 저장한 값은
다시 볼 수 없으므로** 각 서비스 콘솔에서 새로 받아야 한다.

| 이름 | 어디서 |
|---|---|
| `NEXTAUTH_URL` | 로컬은 `http://localhost:3000` |
| `NEXTAUTH_SECRET` | 아무 긴 문자열. 바꾸면 기존 로그인 세션이 다 끊긴다 |
| `KAKAO_CLIENT_ID` | 카카오 개발자 → 내 앱 → 앱 키 → **REST API 키** |
| `KAKAO_CLIENT_SECRET` | 카카오 개발자 → 카카오 로그인 → 보안 |
| `FIREBASE_PROJECT_ID` / `FIREBASE_CLIENT_EMAIL` / `FIREBASE_PRIVATE_KEY` | Firebase 콘솔 → 프로젝트 설정 → 서비스 계정 → 새 비공개 키 |
| `ADMIN_PASSWORD_HASH` | `/admin/setup` 페이지에서 새로 만든다 |
| `ODSAY_API_KEY` | lab.odsay.com → 내 애플리케이션 |

`FIREBASE_PRIVATE_KEY`는 줄바꿈이 들어 있어 `"..."`로 감싸야 한다.

```bash
npm run dev        # http://localhost:3000
npx tsc --noEmit   # 타입 검사 — 커밋 전에 항상
npm run build      # 빌드 확인
node --env-file=.env.local scripts/check-odsay.mjs   # 대중교통 API 점검
```

## 보안 — 반드시 지킬 것

이 프로젝트는 처음에 **Firestore가 전부 공개**였고, 이를 참여자 전용으로
바꾸는 7단계 작업을 마쳤다. 그 구조를 무너뜨리지 말 것.

- **클라이언트 쓰기는 전면 금지.** 모든 쓰기는 서버 API(`/api/promises/*`)를 거친다.
  `firestore.rules`가 클라이언트 write를 막고 있다.
- **약속 비밀번호는 해시로만** `promises/{id}/private/auth`에 있다. 보안 규칙은
  필드 단위로 가릴 수 없어서, 약속 문서에 두면 문서를 읽는 순간 같이 읽힌다.
  이 하위 컬렉션은 규칙에서 완전히 차단되어 있고 Admin SDK만 접근한다.
  **어떤 API 응답에도 해시를 넣지 말 것.**
- **참여자 상태(`members/`)도 쓰기는 서버만.** 읽기는 같은 약속 참여자 전원에게
  연다(서로 어떻게 오는지 보는 게 목적이라 본인 것만 읽게 하면 의미가 없다).
  하위 컬렉션은 부모 조건을 물려받지 않아서, 규칙이 부모 약속을 `get()`으로
  직접 읽어 참여자인지 본다. 대상은 늘 요청자 본인이라 API 경로에 uid를 받지
  않는다 — 그래서 URL만으로 남의 상태를 건드릴 방법이 없다.
- **읽기는 참여자만.** 대시보드는 `where("participantIds","array-contains",uid)`로
  읽는다. 규칙은 필터가 아니라서, 목록 질의에 한 건이라도 권한 없는 문서가
  걸리면 질의 전체가 실패한다.
- **외부 API 키는 서버에만.** 카카오·ODsay 호출은 전부 서버 라우트를 거친다.
  브라우저에서 직접 부르면 키가 노출되고 남이 쿼터를 대신 쓴다.
- **`.env.local`은 절대 커밋하지 않는다.** `.migration-backup/`도 마찬가지다
  (평문 비밀번호가 들어 있다).
- **관리자 비밀번호는 사람이 정한다.** `/admin/setup`에서 해시만 만들어 환경변수에
  넣는 방식이고, 서버는 평문을 저장하지 않는다.

## uid 규칙

사용자 식별자는 **항상 `kakao:숫자`** 형식이다. NextAuth JWT가 로그인 시점에만
`token.uid`를 채워서, 그 전에 발급된 토큰은 접두사 없는 값으로 흘러온다.
그래서 **모든 uid는 `lib/uid.ts`의 `toCanonicalUid()`를 통과시킨다.** 이걸
빼먹으면 해당 브라우저가 조용히 자기 약속에서 잠긴다.

## 길찾기 — 한국에서 겪은 것들

**구글 길찾기는 한국에서 못 쓴다** (지도 데이터 반출 규제). 그래서:

| | 자동차 | 대중교통 |
|---|---|---|
| 카카오모빌리티 | ✅ 하루 1만 건 무료 | ⚠️ 제휴 계약 필요 (아래) |
| 네이버 클라우드 | ✅ | ❌ 자동차만 |
| ODsay | ❌ | ✅ 하루 1천 건 무료 |
| TMAP | ✅ | ✅ 비쌈 |

**카카오모빌리티 대중교통은 "없는" 게 아니라 "제휴 전용"이다.** 한동안 이
문서에 "API 없음"이라고 잘못 적혀 있었다. 2026-08-19에 개발자 문서를 직접
열어 확인한 사실은 이렇다.

- 누구나 키만 받아 쓰는 **공개 길찾기 API는 자동차뿐**이다. 엔드포인트 5개
  (`/v1/directions`, `/v1/waypoints/directions`, `/v1/origins/directions`,
  `/v1/destinations/directions`, `/v1/future/directions`)가 전부 자동차 기반이다.
- **제휴(Partnership) API에는 대중교통이 있다.**
  `apis-navi.kakaomobility.com/affiliate/publictransit/v1/multimodal/directions`
  (대중교통 통합 길찾기). 도보·자전거·지오코딩도 같은 제휴 전용이다.
- 쓰려면 카카오비즈니스 계정을 만들고 **사업자명**을 포함한 앱 정보를
  `tech.partners@kakaomobility.com`으로 보내 승인을 받아야 한다.
  개인 프로젝트로는 사실상 문턱을 넘기 어려워 ODsay를 그대로 쓴다.
- 나중에 사업자로 전환할 일이 생기면 여기를 다시 볼 것 — 자동차와 대중교통을
  한 곳에서 받으면 키도 하나, 좌표계도 하나로 줄어든다.

(네이버·TMAP 칸은 이번에 다시 확인하지 않았다. 예전에 조사한 내용 그대로다.)

- 자동차는 **카카오모빌리티**. `KAKAO_CLIENT_ID`가 곧 REST 키라 별도 발급이 없다.
- 대중교통은 **ODsay**. Server 방식으로 등록하면 공인 IP 고정을 요구하는데
  Vercel은 요청마다 IP가 달라(고정 IP는 월 $100, Pro 이상) 맞출 수 없다.
  그래서 **URI 방식**으로 등록하고 서버가 `Referer`를 직접 붙여 보낸다.

### ODsay에서 크게 데인 것 (다시 밟지 말 것)

1. **도시 안(SearchType=0)과 도시 간(1)은 응답 필드 이름이 다르다.**
   요금 `payment` ↔ `totalPayment`, 환승 `bus/subwayTransitCount` ↔ `transitCount`,
   경로 종류 `pathType` 1~3 ↔ 11~13.
2. **`outTrafficCheck=1`은 "도시 경계를 넘는다"는 정보일 뿐**,
   "도시 안 결과를 버리라"는 뜻이 아니다. 이걸 오해해서 안양→잠실이
   **62분 대신 11분**으로 나왔다(도시 간 검색이 준 "안양→영등포"였다).
3. **도시 간 검색은 목적지에 닿지 않는 경로를 준다.** 역과 역 사이만 안내하기
   때문이다. 이동 거리로도 못 거른다(안양→서울역은 직선거리의 1.21배).
   그래서 **직선거리 40km**로 나눈다 — 도시 안은 늘 부르고, 40km가 넘을 때만
   도시 간을 보탠다.
4. **항공·해운은 뺀다.** 서울→부산이 "65분"으로 나오는데 김포→김해 비행기다.
   공항까지 가는 시간이 없어 이걸 믿고 약속을 잡으면 안 된다.
5. **같은 길에 버스 번호만 다른 경로를 여러 개 준다**(5531/51/5623).
   타는 곳·내리는 곳이 같으면 한 줄로 묶고 번호를 나란히 붙인다.
6. **오류 메시지는 `msg`가 아니라 `message`다.** 그리고 실패해도 HTTP는
   200으로 오고 본문에 `error`가 들어온다. `msg`를 읽고 있어서
   "[ApiKeyAuthFailed] ApiKey authentication failed."가 통째로 사라지고
   코드 `500`만 로그에 남았고, 그 500을 HTTP 상태 코드로 착각해 리전과
   User-Agent를 고치는 헛발질을 두 번 했다. **오류는 객체째로 찍을 것.**
7. **환경변수에 섞인 공백이 키를 죽인다.** Vercel에 붙여넣은 ODsay 키 앞에
   탭 문자가 하나 딸려 들어가 22자가 23자가 되었고 인증이 거절됐다.
   로컬 `.env.local`은 멀쩡해서 **"로컬은 되는데 배포판만 안 되는"** 증상으로
   나타났다. 지금은 `lib/directions.ts`의 `readApiKey()`가 앞뒤 공백을
   떼어낸다. 같은 증상을 만나면 **키 앞 4글자와 길이부터 로그로 갈라볼 것** —
   추측보다 훨씬 빠르다.

`scripts/check-odsay.mjs`가 앞의 네 가지를 한 번에 점검한다
(성수→강남 / 안양→롯데월드 / 서울→부산 / 서울→대전). **대중교통 쪽을
건드리면 반드시 돌려볼 것.** 키는 앞 4글자만 찍으므로 결과를 그대로 공유해도 된다.

## 파일 지도

```
lib/
  uid.ts              uid 정규화 — 모든 uid는 여기를 통과
  api-guard.ts        API 인증. NextAuth 쿠키 + Bearer 토큰(향후 RN 앱용)
  promise-service.ts  Admin SDK 도메인 로직 (생성·참여·탈퇴·삭제·참여자 상태)
  password.ts         약속 비밀번호(scrypt, 4자 이상)
  admin-password.ts   관리자 비밀번호. 구분자가 ':' 인 이유는 아래 참고
  admin-auth.ts       HMAC 서명 httpOnly 쿠키 pr_admin (8시간)
  promise-time.ts     D-day·정렬·표시용 시간 계산
  directions.ts       카카오모빌리티 자동차 길찾기 (+ 경로 좌표)
  transit.ts          ODsay 대중교통 경로 + 구간 단계
  transit-lane.ts     ODsay loadLane — 지도에 그릴 노선 좌표
  my-places.ts        저장한 출발지. 지금은 localStorage(아래 "남은 일" 참고)
  apple-shape.ts      사과 모양의 유일한 정의처 (워드마크·탭바·게이지가 공유)
  brix.ts             당도 8~20, 단계·독사과. STAGES가 유일한 정의처
  meeting-mode.ts     직접/온라인 판단 + 링크 안전성. 온라인 판단은 여기서만
  place-compare.ts    장소 후보 집계(평균·최장·편차)와 한 줄 판정
  place-service.ts    장소 계산·변경·제안 (Admin SDK)
  date-vote.ts        날짜 후보 집계·정렬·판정
  date-service.ts     날짜 후보·투표·확정 (Admin SDK)
components/
  promise-ticket.tsx  약속 티켓. example 모드는 누를 수 없는 예시용
  promise-map.tsx     카카오 지도 + 경로 선 + 승하차 점
  travel-time.tsx     출발지 고르기 → 경로 목록 → 지도에 그리기 → 서버에 저장
  origin-search.tsx   출발지 검색창 (카카오 장소 검색)
  member-board.tsx    누가 뭘 타고 오는지 + 확인/가는 중/도착 (실시간 구독)
  empty-promises.tsx  약속 0건일 때 화면 — 대시보드와 관리자가 공유
  departure-block.tsx "나가야 하는 시각" 카드
  apple-gauge.tsx     사과 + 당도 링. 벌레/황금/독사과 변형
  origin-picker.tsx   장소 미정일 때 출발지만 정하는 칸 (+ 정한 사람 N/M)
  place-compare.tsx   후보 검색 → 전원 이동시간 → 제안/변경
  place-suggestions.tsx  올라온 장소 제안 목록
  date-vote.tsx       날짜 후보 + O/△/X 투표 + 확정
  online-meeting-card.tsx  온라인 플랜의 링크 카드
app/
  page.tsx                    대시보드 (내 약속만)
  promise/[promiseId]/        약속 상세
  admin/                      테스트 관찰용 관리자 화면 (읽기 전용)
  api/directions/             소요시간 · /lane 은 노선 좌표
  api/promises/[id]/me        이 약속에서의 "나" — 경로·출발지·상태 (PATCH)
  api/promises/[id]/place     후보 계산(POST) · 장소 변경(PATCH, 만든 사람만)
  api/promises/[id]/place/suggestions  제안 올리기(POST)·거두기(DELETE)
  api/promises/[id]/dates     후보(POST)·투표(PATCH)·확정(PUT)·거두기(DELETE)
scripts/check-odsay.mjs       대중교통 API 점검
scripts/verify-rules.mjs      배포된 보안 규칙을 클라이언트 SDK로 실제 검증
```

**함정 하나:** Next.js의 `.env` 파일은 `$1` 같은 값을 변수로 보고 치환한다.
그래서 `admin-password.ts`는 scrypt 파라미터 구분자로 `$`가 아니라 `:`를 쓰고
base64url로 인코딩한다. 이걸 모르고 `$`를 쓰면 해시가 조용히 깨진다.

## 사람이 직접 해야 하는 일

Claude가 못 하는 것들이다.

- **Firestore 보안 규칙 배포** — `firebase` CLI가 없어서 Firebase 콘솔에
  붙여넣어야 한다. 배포 전에 이전 내용을 백업하고, 콘솔 Playground로 먼저 검증할 것.
  `members/` 읽기 규칙까지 배포·검증 완료(`node scripts/verify-rules.mjs` 16/16 통과).
- **Firestore 색인 생성** — `participantIds`(array-contains) + `createdAt`(내림차순).
- **외부 서비스 가입과 키 발급** (카카오·Firebase·ODsay·Vercel).
- **환경변수 등록 후 Vercel 재배포** — 저장만 하면 기존 배포본에는 적용되지 않는다.
- **이름을 어디까지 바꿀까** — 코드 안의 "Promise Room"은 Applan으로 다 바꿨다.
  인프라 셋 중 하나는 옮겼고 둘은 남았다.
  - **Vercel 프로젝트/도메인** — ✅ **2026-08-21에 `applan`으로 옮겼다.**
    걱정했던 "초대 링크가 깨진다"는 문제는 옛 도메인을 지우지 않는 것으로
    풀었다. 두 주소가 같이 산다. 아래 "도메인 변경" 절 참고.
  - **GitHub 저장소명**(`byeongooh/promise-room`) — 아직 미정. `gh repo rename`
    으로 가능은 하지만, 이미 안 사람이 있는 링크·클론이 있으면 혼란을 준다.
    (GitHub은 옛 이름으로 온 요청을 새 이름으로 넘겨주므로 Vercel보다는
     덜 위험하다. 다만 확인해보고 결정할 것.)
  - **Firebase 프로젝트**(`promise-room`) — 프로젝트 ID는 애초에 이름을
    바꿀 방법이 없다(구글 정책). 이름을 진짜 바꾸려면 새 프로젝트를 만들고
    Firestore 문서·인증·보안 규칙을 통째로 옮겨야 하는 큰 작업이다.
    **`lib/firebase.ts`의 설정은 손대지 않는다.**

## 언제·어디서를 맞추는 방식 (2026-08-19 추가)

플랜은 **날짜·장소를 비워둔 채로 만들 수 있다.** 방부터 열고 안에서 맞추는
흐름이 실제로 더 흔해서다. 화면에서는 "미정 / 정하는 중"으로 읽는다 —
"—"처럼 비워두면 데이터가 빠진 것처럼 보이는데, 이건 빠진 게 아니라 아직
고르는 단계라는 상태다.

**장소는 숫자로, 날짜는 표로 정한다.** 장소는 이동시간이라는 객관적인 값이
있어서 줄을 세울 수 있지만, 날짜는 각자에게 물어보는 수밖에 없다.

두 기준 모두 **다수결이 아니다.** 이게 이 기능의 핵심 판단이다.

- **장소** — 평균 하나로 정하지 않는다. 평균·제일 먼 사람(최장)·편차 셋을
  같이 본다. 평균만 보면 한 사람이 크게 손해봐도 좋아 보인다. 실제 값으로
  확인한 예: 시청→강남은 평균이 3분 줄지만 한 명이 49분에서 52분으로
  늘어난다. 시청→여의도는 셋 다 준다. `verdict()`는 제일 먼 사람이 더
  힘들어지는 후보를 평균이 좋아져도 추천하지 않는다 — 모임에서 한 명만
  유독 멀면 그 사람이 안 온다.
- **날짜** — 못 오는 사람이 한 명이라도 있으면 표가 많아도 뒤로 민다.
  검산: 돼요 4표·못 옴 1명인 날이 돼요 5표인 날에 밀려 꼴찌가 된다.
  모임은 참석률이 아니라 "그날 우리가 만나는가"의 문제다.
  "애매해요"를 남겨둔 이유 — 억지로 되냐/안 되냐로 가르면 일단 "돼요"를
  누르고 나중에 못 온다.

> **순위에서 뒤로 미는 것과 확정을 막는 것은 다르다 (2026-08-22).**
> "못 오는 사람이 있으면 뒤로 민다"를 확정 버튼까지 끌고 갔다가 한 번
> 뒤집었다. 한 명 못 온다고 나머지가 못 만나는 건 아니고, **"안 돼요"는
> 원래 못 간다고 알리라고 만든 표다.** 그걸 누른 사람이 잘못한 것처럼
> 읽히면 다음부터 아무도 안 누르고 그냥 안 나타난다 — 그게 훨씬 나쁘다.
>
> 그래서 확정 버튼은 **막지 않고 무게만 조절한다.** 되는 사람이 있으면
> "4명이 되는 날로 정하기"처럼 사실을 말해주고(보통 버튼), 확실히 오는
> 사람이 하나도 없을 때만 흐리게 하고 한 번 더 묻는다. 판단은
> `lib/date-vote.ts`의 `confirmStance` 한 곳에 있고 18개 경우로 검산했다.

**권한은 계산과 확정을 나눈다.** 후보를 계산하고 올리는 건 참여자 누구나
(아무것도 바꾸지 않으므로), 실제로 정하는 것만 만든 사람이다. 장소가 아무나
바뀌면 이미 출발한 사람이 헛걸음한다.

**출발지는 경로와 따로 저장한다**(`members/{uid}.origin`). 경로를 골라야만
저장되면 이제 막 들어온 사람이 장소 계산에서 빠지는데, 표본이 한둘이면
"다 같이 편한 곳"이라는 말 자체가 성립하지 않는다. 장소 미정이면 "어떻게
갈까" 칸이 안 나오므로 `origin-picker.tsx`가 그 자리를 대신한다. 옛 문서는
`route.origin`으로 물러서서 읽는다.

**온라인 플랜은 이동시간이라는 개념이 없다.** 그래서 출발지·경로·나가야
하는 시각·장소 비교가 통째로 빠진다. 이 판단은 `lib/meeting-mode.ts`
한 곳에서만 한다 — 화면마다 각자 판단하면 어긋난다. 링크는 사용자 입력을
누를 수 있게 그리는 자리라 `safeMeetingUrl()`이 http(s)만 통과시키고
(`javascript:`·`data:` 차단), 새 창은 `rel="noopener noreferrer"`로 연다.

**제안·날짜 후보는 문서 안 배열이다**(`placeSuggestions`, `dateOptions`).
하위 컬렉션이면 보안 규칙을 새로 짜서 콘솔에 배포해야 하는데(사람이 직접
해야 하는 일), 한 플랜에 몇 건 수준이라 문서에 실어도 무겁지 않다.
`favoritedBy`와 같은 판단이다.

**외부 API 사용량** — 후보 한 곳을 계산할 때 참여자 수만큼 ODsay를 부른다.
하루 1천 건이라 5명 기준 하루 200번쯤이 한계다. 그래서 자동으로 부르지
않고(검색 결과에서 한 곳을 고른 순간에만), 한 사람당 한 방식만 부르고,
제안 목록은 저장해둔 요약을 보여주고 다시 계산하지 않는다.
**캐시는 아직 없다** — 실제 사용량을 보고 정하기로 했다.

## 지금까지 정한 것

- **워드마크**: `Appl🍎n` — 다섯 번째 글자 a를 사과 그림으로. `components/wordmark.tsx`.
  같은 사과 path를 `AppleGauge`·탭바 아이콘과 공유한다.
- **로그인 화면**: 가짜 지도/티켓 배경 두 안을 두고 고르던 것을 걷어내고
  200px 사과 게이지 + 워드마크로 교체(확정). `?bg=` 전환 코드도 삭제됨.
- **색**: 네이비+골드 → 중립 회색 + 사과 빨강(`--ap-red`). 골드는 카카오
  노랑과 부딪혀서 뺐다. 주요 버튼은 잉크색.
- **지각 벌칙(자유 텍스트) 폐지**: 만드는 사람이 적던 "늦으면 ○○" 문구를
  완전히 없앴다. 그 자리는 수확에서 나오는 독사과가 대신할 예정이다
  (수확 자체는 아직 없다 — 아래 "다음" 참고).
- **"가는 중"은 수동 클릭.** 자동 감지가 아니라, 사용자가 플랜을 인지했다는
  확인 신호로 쓰기 위해서다.
- **사과 당도(Brix)**: 8~20, 시작값 13.0. 단계는 새싹(8)·묘목(11)·첫 열매(13)·
  과수(15)·고목(18) — `lib/brix.ts`의 `STAGES`가 유일한 정의처다.
- **독사과**: 당도를 깎지 않고 상승 속도만 절반으로 줄인다. 90일 뒤 자동 소멸.

## 이 앱을 어디까지 가져갈 것인가 (2026-08-21 확인)

**한 줄로: 약속 + 캘린더 + 지도를 한 앱에서, 늦지 않게.**
따로 쓰던 셋(카톡으로 약속, 캘린더에 캘박, 지도 앱으로 길찾기)을 하나로 모으는
것이 이 앱의 자리다. 기능을 더할지 말지는 이 문장에 붙는지로 판단한다.

**목표는 실제 출시다.** 지인 → 학교 → 광고 순으로 넓히고, **앱스토어·플레이
스토어에 실제로 올려 사용자 후기를 받는 것**이 목표다. 잘 안 되더라도 그
과정과 피드백 자체를 포트폴리오로 쓴다. 즉 포폴과 출시가 배타적이지 않다.

이게 판단에 미치는 영향: **"모르는 사람이 처음 열었을 때 이해되는가"가
이제 실제 기준이다.** 설계 판단이 잘 드러나는 것만으로는 부족하다.

**수익 모델(구상)**
- 광고 — 지도 위아래, 플랜 페이지 사이. 단 **모바일 단일 컬럼(`max-w-lg`)이라
  "사이드"는 없다.** 위아래로 끼워야 하고, 지도를 밀어내면 핵심 기능이 나빠진다.
- 매장 제휴 — 광고를 낸 매장이 장소 검색 상단에 노출.

> **매장 제휴에서 반드시 지킬 선.**
> 검색 결과 상단 노출은 괜찮다(광고 표시는 표시광고법상 의무다). 하지만
> **계산된 판정(`place-compare.ts`의 `verdict()`)에는 절대 섞지 않는다.**
> 이 앱이 파는 것이 "장소를 이동시간이라는 객관적인 숫자로 정한다"인데,
> 돈이 그 숫자를 건드리는 순간 장소 비교 기능 전체가 죽는다.
> 평균·최장·편차는 돈이 못 건드리는 영역으로 남긴다.

**당도는 평판 신호다 (2026-08-21 방향 전환)**
당근마켓 매너온도처럼 **"이 사람과 약속을 잡아도 될까"를 판단할 수 있는 값**이
목표다. 그래서 독사과가 당도를 실제로 깎는 쪽으로 바꿨다 — 자세한 이유는
`lib/brix.ts`의 `poisonPenalty` 주석 참고. 올라가기만 하는 값은 시간이 지나면
누구나 높아져서 평판으로 쓸 수 없다.

**스토어 출시가 요구하는 것 — 아직 하나도 없다**
수확을 끝내고 스토어를 준비할 때 필요한데, ①은 인증 구조에 영향이 커서
나중에 알면 비싸다.
1. **카카오 로그인만으로는 App Store 심사를 못 넘길 수 있다.** 가이드라인 4.8이
   제3자 로그인만 제공하는 앱에 다른 로그인 수단을 요구한다(이름·이메일만
   수집하고 이메일 비공개가 가능할 것). Sign in with Apple이 그 조건을 만족한다.
2. **계정 삭제 기능이 없다.** 플랜 삭제와 참여 취소만 있다. Apple은 앱 안에서
   계정을 지울 수 있어야 한다고 요구한다.
3. **개인정보처리방침**이 없다. 두 스토어 다 필수고, 광고 SDK를 넣으면
   수집 항목 신고가 더 붙는다.

## 남은 일

**개인 달력 — 만들었다 (2026-08-22)**
`/calendar`. 탭바에 들어갔다. "캘박"(캘린더에 박아둔다) 문화를 앱 안으로
가져온 것으로, 한 명이 플랜을 만들면 참여자 **모두의 달력에 자동으로 들어가고**,
달력에서 누르면 플랜 상세로 간다. 개인 메모도 같은 달력에 남긴다.

**만들기 쉬운 이유 — 데이터가 이미 다 있다.** 플랜에 날짜·시각이 있고,
대시보드가 이미 `where("participantIds","array-contains",uid)`로 내 플랜을
전부 읽는다. 달력은 **새 데이터 파이프라인이 아니라 같은 데이터의 다른 화면**이다.
정렬도 `promise-time.ts`의 것을 그대로 쓴다.

> **이 달력이 이겨야 하는 상대는 기본 캘린더다.**
> 폰에 이미 있고 알림도 잘 되는 걸 두고 왜 이걸 여느냐 — 답은 하나뿐이다.
> **기본 캘린더는 "6시 강남역"까지만 알고, "4시 15분에 나가야 한다"를 모른다.**
> 그러니 달력 항목에 약속 시각이 아니라 **출발 시각**이 같이 보여야 한다.
> 그게 안 보이면 이 달력은 존재 이유가 없다.

만든 것:
- `lib/calendar.ts` — 월 그리드·날짜별 묶기. 순수 함수라 화면 없이 검산했다
  (26개 경우: 현지 기준 날짜 열쇠, 윤년, 연말 넘김, 같은 날 여러 건 정렬).
- `components/calendar-month.tsx` · `calendar-day.tsx` · `note-composer.tsx` ·
  `app/calendar/page.tsx`
- 메모는 `users/{uid}/notes/{id}` 하위 컬렉션. 배열로 두면 계속 쌓여 문서가
  무거워진다 — `placeSuggestions`와 판단이 다른 이유가 이것이다.

**메모를 적는 방식 — 세 안을 견주고 B+C로 갔다.**
디자인 캔버스(`design/`)에서 A(한 줄 입력) · B(자주 쓰는 칩) · C(약속 안
체크리스트) 세 가지를 서로 다른 축으로 놓고 봤다. 적는 게 원래 귀찮은 일이라
어느 하나로는 안 풀린다.

- **C가 기본 자리다.** 약속 카드 안에 "챙길 것" 체크리스트를 둔다. 약속을
  보다가 그 자리에서 적는 게 제일 안 귀찮다 — 화면을 옮기게 하면 안 적는다.
- **B를 그 위에 얹었다.** 자주 쓰는 문구를 칩으로 띄워 한 번 누르면 담긴다.
  **목록을 박아두지 않고 본인 이력에서 뽑는다**(`frequentTexts`) — 자주 적는
  것은 사람마다 달라서 남이 정해준 목록은 결국 안 쓰인다. 처음 쓰는 사람에게만
  기본값 네 개를 보여주고, 이력이 쌓이면 밀려난다.
- 칩은 늘 펼쳐두지 않는다. **아직 아무것도 안 담았을 때**와 **입력칸을 눌렀을
  때**만 편다 — 한 화면에 이 조각이 둘 이상 놓일 수 있어서(약속마다 하나 +
  그날 하나) 칩 줄이 여러 개면 시끄럽다.

> **약속이 없는 날에도 메모가 된다.** 달력을 여는 이유가 약속만은 아니다.
> `promiseId`가 없는 메모가 그날 메모이고, 약속이 하나도 없는 날에도 "메모"
> 칸이 그대로 나온다. 이걸 빼면 이 화면은 반쪽이다.

체크는 누르는 즉시 화면이 바뀌고 서버 왕복은 뒤따른다. 실패하면 되돌려서
화면과 서버가 어긋난 채로 남지 않는다.

> **보안 규칙을 배포하지 않아도 됐다 — 읽기까지 서버로 돌려서.**
> 이 프로젝트는 읽기를 클라이언트가 Firestore에서 직접 하는 게 기본인데
> (참여자 상태처럼 실시간이어야 하는 게 있어서), 메모는 나만 쓰고 나만 본다.
> 실시간일 이유가 없으니 `/api/notes`로 읽고 쓴다. 그러면 `users/` 규칙을
> 새로 짜서 콘솔에 배포하는 일(사람이 직접 해야 하는 일)이 통째로 사라진다.
> **수확에서 `users/{uid}` 문서가 필요해지면 그때 한 번에 만든다.**

**출발 시각은 플랜마다 따로 읽는다.** `promises/{id}/members/{uid}`에 있어서
플랜 목록 질의에는 안 딸려온다. `collectionGroup`으로 한 번에 가져오는 방법도
있지만 색인과 규칙을 새로 배포해야 해서, 지금 규모(한 사람당 수십 건)에서는
플랜마다 한 번씩 읽는 편이 싸다. 경로를 아직 안 고른 플랜은 값이 없어서
"경로를 정하면 알려드려요"로 대신 보여준다.

> **달력은 탭바 항목이 아니라 홈 안의 전환이다 (2026-08-22에 되돌림).**
> 처음엔 탭바에 네 번째 항목으로 넣었다. 그랬더니 목적지가 셋이 되면서
> 가운데 ➕를 기준으로 좌우가 안 맞았고, 오른쪽에 `flex-[2]`를 주는 식으로
> 억지로 맞췄다. **그건 배치 문제가 아니라 애초에 나누지 말았어야 할 것을
> 나눈 결과였다** — 달력은 홈과 다른 곳이 아니라 같은 플랜을 다르게 보는
> 방식이다.
>
> 지금은 홈 상단의 전환(`components/view-switch.tsx`)이고 탭바는 셋으로
> 돌아왔다(홈·새 플랜·내 사과). 탭바의 "홈"은 `/calendar`에서도 켜져 있다.
>
> 이름은 **"목록 / 달력"**이다. "일정"은 달력도 일정을 보여줘서 구분이
> 안 된다. 이 둘은 무엇을 보느냐가 아니라 **어떻게 보느냐**가 다르다.
> 주소는 `/`와 `/calendar`로 나눠 둔다 — 한 주소에서 상태로 가르면 달력을
> 보다가 새로고침했을 때 목록으로 돌아가고, 링크로 건네줄 수도 없다.

**함정 — 날짜 타입이 섞여 있다.** `PromiseData.date`가 `string | Timestamp`다.
Firestore는 타입별로 먼저 정렬해서 **문자열 범위 질의가 Timestamp 문서를
통째로 놓친다.** 지금은 한 사람의 플랜이 수십 건 수준이라 **전부 읽어서
클라이언트에서 월별로 묶는 것이 맞다** — 색인도 마이그레이션도 필요 없다.
규모가 커지면 그때 날짜를 문자열로 통일하고 색인을 만든다.

**나중에 — 진짜 캘린더로 내보내기.** `.ics` 한 건 내보내기는 쉽고, 구독
피드(webcal)로 자동 동기화도 가능하다. RN 앱에서는 네이티브 캘린더 권한으로
진짜 캘박이 된다. 다만 **먼저 앱 안 달력이 기본 캘린더보다 나은 이유(출발
시각)를 만들어야** 내보내기가 의미가 있다.

**그다음 — 수확(하러스트)**
지금 껍데기만 있다. 플랜이 끝나면 참여자끼리 "제시간에 왔는지" 서로 평가하고,
그 결과로 당도가 오르거나 독사과가 달리는 흐름 전체가 아직 없다. 필요한 것:
- 사용자 문서(`brix`, `poisonApples[]`) — 지금 `/me` 화면은 이 값이 없어서
  모두 시작값(13.0)만 보여주고 있다(`hooks/use-apple-stats.ts`에 이미 주석으로
  적어둠).
- 투표를 담을 하위 컬렉션 + **"전원 제출 전에는 서로 못 본다"는 보안 규칙**.
  이게 없으면 담합·눈치보기를 못 막는다.
- 당도 계산 서버 로직(조작 방지 규칙 포함).
- 화면 4장: 홈 수확 배너 · 평가하기 · 제출 후 대기 · 결과.

그다음이 알림이다. 서버가 경로를 저장할 때 `leaveAt`(= 약속시각 − 소요시간)을
같이 박아둔다. "몇 분 전에 보낼지"만 정하면 알림을 걸 수 있다.
그 뒤가 좌표를 얹는 실시간 위치 공유다.

**규칙 배포 — 완료.** `promises/{id}/members/{uid}` 읽기 규칙까지 배포하고
`node scripts/verify-rules.mjs`로 16개 케이스 전부 통과 확인함.

**확인 끝난 것** (2026-08-17)
- **ODsay 실시간 버스 도착 — 낮에는 정상이다.** 서울역버스환승센터에서
  22개 노선 중 19개에 도착시간이 왔다. 필드는 `real[].routeNm`,
  `arrival1.arrivalSec`(초), `leftStation`(남은 정거장), `congestion`,
  `lowBusYn`(저상). 새벽에 비어 보였던 건 버스가 안 다녀서였다.
  단 **정류장에 따라 아예 빈 응답도 온다** (홍대입구역 ID 83422가 그랬다).
  붙일 때 빈 응답을 정상으로 처리할 것.
- **TMAP 보행자 경로 = 무료 1,000건/일.** `경로안내` API 그룹(자동차·보행자·
  타임머신·이미지·화물차 공용) 한도이고, 초과하면 **과금이 아니라 자동 차단**된다.
  종량제는 11원/건, 정액제는 월 220만원(1만건/일). 이 프로젝트는 자동차를
  카카오로 쓰므로 1,000건을 전부 보행자에 쓸 수 있다. ODsay와 같은 한도다.
  (역시 비싼 건 TMAP **대중교통** API 쪽이고 보행자는 별개가 맞았다.)

**로그인 화면을 볼 수 있게 됐다 (2026-08-21)**
오랫동안 "카카오 로그인이 필요한 화면은 Claude가 볼 수 없다"가 이 프로젝트의
가장 큰 제약이었다. `playwright-cli`를 붙여서 풀었다.

```bash
npm install -g @playwright/cli@latest
playwright-cli install --skills
# 이름 붙인 세션 + 창 보이기 + 프로필 유지 → 사람이 한 번만 로그인하면 남는다
playwright-cli -s=applan open http://localhost:3000/login --headed --persistent
```

`--persistent`가 핵심이다. 이게 없으면 프로필이 메모리에만 있어서 로그인이
안 남고, `--headed`가 없으면 창이 안 보여서 사람이 로그인할 수도 없다.
프로필은 `%LOCALAPPDATA%\ms-playwright\daemon\...\ud-applan-chrome`에 저장된다
(저장소 밖이라 깃에 안 올라간다). 절차는 `.claude/skills/visual-check` 참고.

**`.playwright-cli/`는 반드시 .gitignore에 둔다** — 실행할 때마다 스크린샷과
콘솔 로그를 프로젝트 폴더에 쌓는데, **로그인한 화면을 찍은 스크린샷이 그대로
저장소에 올라간다.**

**여러 명 플랜을 실데이터로 밟았다 (2026-08-21)**
"참여자가 전부 1인이라 못 밟아봤다"던 것이 풀렸다. 2인 플랜("다음주 어디서
보징" · 이정은/김병오 · 해운대)에서 확인한 것:

- 참여자마다 **다른 출발지에서 다른 출발 시각**이 나온다.
  이정은 영통역에서 4시간 11분 → 16:48 출발 / 김병오 안양에서 4시간 44분 → 16:15 출발.
  이 앱이 파는 값이 여러 명에서 실제로 도는 것을 처음 확인했다.
- 상태 버튼이 **당일 아침에 열린다**는 것도 확인. D-3이라 버튼 대신
  "확인 · 가는 중 · 도착은 약속 당일 아침에 열려요"가 떠 있다.
- 알림 시각이 제대로 계산된다. 21:00 약속 → 아침 9:00, 출발 20:00.
- 방장이 아닌 사람에게는 확정 바가 안 보인다(이의가 없을 때). 의도대로다.
- 안양→해운대 대중교통에 **비행기가 안 섞인다.** 기차 2시간 10분,
  고속버스 4시간. 예전에 데인 것이 재발하지 않았다.

아직 못 밟은 것: **장소 비교·날짜 투표**는 이 플랜이 이미 확정 상태라
화면에서 물러나 있다. 정하는 중인 여러 명 플랜을 만들어야 볼 수 있다.

**도메인 변경 — 끝났다 (2026-08-21)**
`promise-room` → `applan`. 프로덕션은 이제 **https://applan.vercel.app** 이다.
1. ✅ 카카오 콘솔 — 앱 > 플랫폼 키에서 **REST API 키의 리다이렉트 URI**와
   **JS 키의 JS SDK 도메인 + 리다이렉트 URI**에 `applan.vercel.app` 추가 완료.
   (리다이렉트 URI 위치가 예전 문서와 다르다. "카카오 로그인 > 고급"이 아니라
    **앱 > 플랫폼 키** 안이다. 새 키를 만들지 말고 기존 Default 키를 수정할 것 —
    새로 만들면 `KAKAO_CLIENT_ID`와 안 맞아 로그인이 통째로 깨진다.)
2. ✅ Vercel 환경변수 `NEXTAUTH_URL` → `https://applan.vercel.app`
3. ✅ Vercel Settings > General > Project Name → `applan`
4. ✅ **Settings > Domains에서 `applan.vercel.app`을 직접 붙였다** — 아래 참고
5. ✅ Redeploy (환경변수는 새 빌드부터 적용된다)
6. ⬜ 나중에 ODsay 콘솔 등록 URI 변경 + `ODSAY_REGISTERED_URI` 설정

**프로젝트 이름을 바꿔도 도메인은 안 따라온다.** 원래 계획에는 4번이 없었다.
3번만 하면 `.vercel.app` 주소도 같이 바뀔 줄 알았는데, Vercel은 프로젝트
이름과 도메인을 따로 관리한다. 이름은 `applan`이 됐는데 서비스는 계속
`promise-room.vercel.app`에서 돌고 `applan.vercel.app`은 404였다.

이 상태에서 재배포했으면 **로그인이 통째로 깨질 뻔했다.** `NEXTAUTH_URL`은
이미 `applan.vercel.app`으로 바꿔둔 뒤라, 카카오에서 돌아올 곳이 404인
주소가 된다. 화면은 멀쩡해 보이는데 로그인만 안 되는, 원인 찾기 나쁜 증상이다.
**순서가 중요하다 — 도메인을 먼저 붙이고 재배포는 그다음.**
(Settings > Domains > Add Existing > `applan.vercel.app`. Vercel 자기네
하위 도메인이라 DNS 확인 같은 걸 묻지 않고 바로 붙는다. `Buy`는 돈 내고
진짜 도메인을 사는 버튼이니 누르지 말 것.)

**옛 주소도 그대로 살려뒀다.** `promise-room.vercel.app`을 지우지 않아서
이미 보낸 초대 링크가 계속 열린다. 단 로그인하는 순간 `applan.vercel.app`
으로 넘어간다 — `NEXTAUTH_URL`이 하나뿐이라 그렇고, 깨진 게 아니다.

**확인한 방법.** `/api/auth/providers`를 부르면 NextAuth가 `NEXTAUTH_URL`로
만든 `signinUrl`·`callbackUrl`을 그대로 돌려준다. 로그인 화면을 눈으로
못 봐도 환경변수가 실제로 들어갔는지 이걸로 갈라볼 수 있다. 두 도메인
모두 `https://applan.vercel.app/api/auth/callback/kakao`를 반환하는 것을
확인했다. **사람이 카카오 로그인을 실제로 눌러 들어가지는 것까지 확인함**
(2026-08-21). 즉 새 도메인에서 로그인은 확실히 된다.

**대중교통은 3번을 해도 안 죽는다.** `odsayReferer()`가 배포 도메인이 아니라
"ODsay에 등록된 주소"를 쓰도록 떼어놨기 때문이다. 그래서 6번은 급하지 않다.

**아직 실사용자가 없다 — 테스트를 아껴 하지 않아도 된다 (2026-08-21 확인)**
Firestore에 들어 있는 플랜은 전부 만든 사람이 직접 넣은 것이고, **앱으로
나가기 전까지 실사용은 없다.** 그러니 화면을 확인하려고 테스트 플랜을 만드는
것을 망설이지 않아도 된다 — 확정 상태·날짜 투표·여러 명 상황처럼 **조건을
만들어야만 보이는 화면**이 많은데, 그때마다 물어보느라 확인을 건너뛰는 게
더 나쁘다. 다 쓴 것은 지우면 된다.

**정리할 것**
- 이름을 어디까지 바꿀까 — 위 "도메인 변경" 참고. GitHub 저장소명은 아직 미정.
- 워드마크 서체 — 지금은 Archivo. 시안엔 다른 후보도 있었는데 미정.
- `my-places.ts`가 localStorage를 쓴다. 앱으로 옮길 때 서버로 이전.
- 프로덕션에 "관리자" 이름으로 만든 샘플 플랜 8건이 남아 있다.
  (김병오 이름의 테스트 플랜 5건은 2026-08-19에 정리했다.)
- 친구들(이시온·김주환·김영주·배지영)이 링크+비밀번호로 다시 참여해야 한다.
  보안 전환 때 이름 기반 참여자가 접근 권한을 잃었기 때문이다.

## 작업 방식

반복되는 절차는 `.claude/skills/`에 스킬로 적어 두었다. 깃에 들어 있어서
어느 컴퓨터에서든 따라온다.

| 스킬 | 언제 |
|---|---|
| `deploy` | 코드를 고쳐 프로덕션에 올릴 때 — 검사 → 커밋 → 푸시 → 배포 확인 |
| `external-api` | 외부 API를 새로 붙이거나 응답 파싱을 고칠 때 |

- 커밋 메시지는 **한국어로, 왜 그렇게 했는지**를 적는다. 무엇을 바꿨는지는
  diff에 있다. 이 저장소의 로그가 사실상 설계 기록이다.
- 코드 주석도 한국어. 문법이 아니라 **판단**을 적는다.
- 커밋 전 `npx tsc --noEmit`과 `npm run build`.
- 사용자는 웹 개발이 처음이다. 전문 용어를 쓸 때는 짧게 풀어 쓴다.
- 확인하지 못한 것은 확인하지 못했다고 말한다. 카카오 로그인이 필요한 화면은
  Claude가 직접 볼 수 없다.
